{
  "name": "sink-batch-req-stock-adj",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "requisition.batch_stock_adjustments",
    "connection.url": "jdbc:postgresql://db:5432/open_lmis",
    "connection.user": "postgres",
    "connection.password": "p@ssw0rd",
    "auto.create": "true",
    "insert.mode": "upsert",
    "pk.fields": "id",
    "pk.mode": "record_key",
    "delete.enabled": "true",
    "max.retries": 100
  }
}
